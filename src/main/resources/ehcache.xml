<?xml version="1.0" encoding="UTF-8"?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="http://ehcache.org/ehcache.xsd">
	
	<!-- 定义缓存内容在磁盘上的存储地 -->
	<diskStore path="/data/ppsellsCache/cache" />
	<!-- <diskStore path="/home/ztx/program/wwmall/cache" />    -->
	<!-- Begin Date:2013-06-26 Name:wuhuanrong  addReason:配置ehcache 端口监听地址和端口，就可以使用ehcache monitor 工具进行监测。  -->
	<!-- 
	使用时可反注释掉。
	<cacheManagerPeerListenerFactory
    class="org.terracotta.ehcachedx.monitor.probe.ProbePeerListenerFactory"
    properties="monitorAddress=localhost, monitorPort=9889" /> 
    -->
    <!-- End Date:2013-06-26 Name:wuhuanrong  addReason:配置ehcache 端口监听地址和端口，就可以使用ehcache monitor 工具进行监测。  -->
    
    <!-- 为相对应的缓存空间进行的细致配置：
    maxElementsInMemory：缓存中允许创建的最大对象数
	eternal：缓存中对象是否为永久的，如果是，超时设置将被忽略，对象从不过期。
	timeToLiveSeconds：缓存数据的生存时间，也就是一个元素从构建到消亡的最大时间间隔值，这只能在元素不是永久驻留时有效，如果该值是0就意味着元素可以停顿无穷长的时间。
	overflowToDisk：内存不足时，是否启用磁盘缓存。
    -->
	<defaultCache maxElementsInMemory="1000" eternal="false" timeToIdleSeconds="120" timeToLiveSeconds="120" overflowToDisk="true" /> 

</ehcache>